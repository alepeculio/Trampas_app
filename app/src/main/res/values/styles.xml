<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlHighlight">#bdbdbd</item>
        <item name="colorControlNormal">@color/colorGris</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Menu personalizado -->
    <style name="AppTheme.Menu">
        <item name="android:windowBackground">@color/colorNegro</item>
        <item name="android:textColorSecondary">@color/colorBlanco</item>
        <item name="android:textColorPrimary">@color/colorBlanco</item>
        <item name="colorPrimary">@color/colorBlanco</item>
        <item name="android:listDivider">@color/colorGrisTransparente</item>
        <item name="colorControlHighlight">@color/colorGrisTransparente</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextAppearance.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

</resources>
